doctype html
html
  head
    title My Authentication App
    link(rel="stylesheet", href="/styles.css")
  body
   header(class="bg-gray-800 p-4")
  nav
    ul
      li
        a(href="/auth/register" class="text-white mr-2 hover:text-gray-200") Register
      li
        a(href="/auth/login" class="text-white mr-2 hover:text-gray-200") Login
    if isAuthenticated
      li
        a(href="/auth/logout" class="text-white hover:text-gray-200") Logout

    block content
